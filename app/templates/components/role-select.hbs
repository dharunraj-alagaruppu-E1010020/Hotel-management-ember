<label for="selectRole">Select Role:</label>
<select id="selectRole" onchange={{action "handleRoleSelection"}}>
    <option disabled selected>Select a role</option>
    {{#each role_obj as |role|}}
      <option value={{role.id}}>{{role.name}}</option>
    {{/each}}
</select>